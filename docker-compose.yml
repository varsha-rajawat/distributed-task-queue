version: "3.9"

services:
  redis:
    image: redis:7-alpine
    container_name: taskqueue_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  postgres:
    image: postgres:15-alpine
    container_name: taskqueue_postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: taskqueue
      POSTGRES_PASSWORD: taskqueue_secret
      POSTGRES_DB: taskqueue_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U taskqueue -d taskqueue_db"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  redis_data:
  postgres_data:
